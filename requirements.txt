# LocalAI — Python Dependencies
# Install with: pip install -r requirements.txt

# ── Local LLM (Ollama interface) ────────────────────────────────────────────
ollama>=0.3.0                    # Python client for local Ollama LLM
requests>=2.32.0                 # HTTP client for Ollama REST API

# ── Embeddings (local, no API key) ──────────────────────────────────────────
sentence-transformers>=3.0.0     # BERT-based local embeddings
torch>=2.2.0                     # PyTorch backend for sentence-transformers

# ── Vector Database (local persistent store) ────────────────────────────────
chromadb>=0.5.0                  # Local vector DB for RAG

# ── Web Data Collection (no API key) ────────────────────────────────────────
duckduckgo-search>=6.2.0         # Free DuckDuckGo web search
trafilatura>=1.10.0              # Web article text extraction
feedparser>=6.0.11               # RSS feed reader
wikipedia>=1.4.0                 # Wikipedia API
beautifulsoup4>=4.12.0           # HTML parsing fallback
lxml>=5.2.0                      # Fast XML/HTML parser for bs4

# ── Scheduling ───────────────────────────────────────────────────────────────
schedule>=1.2.2                  # Lightweight task scheduler
APScheduler>=3.10.4              # Advanced background scheduler

# ── Terminal UI ──────────────────────────────────────────────────────────────
rich>=13.7.0                     # Beautiful terminal output, tables, panels
